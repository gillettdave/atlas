# Copy to .env and fill in real values.
# EXPO_PUBLIC_ prefix makes variables available in client code.

# Dev: your local machine's LAN IP + FastAPI port
# Production: your server's public IP or domain
EXPO_PUBLIC_API_BASE=http://YOUR_SERVER_IP:8000

# Optional — set if your backend uses X-Admin-Token
EXPO_PUBLIC_ADMIN_TOKEN=
